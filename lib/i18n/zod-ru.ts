import { z } from "zod";

// Русские сообщения zod для ошибок валидации, которые API отдаёт в интерфейс.
// Импортируется один раз из lib/db/client.ts, чтобы сработать во всех роутах.
z.config(z.locales.ru());
